Expand cell type coverage from 7 to 24 (full v6 input + CA region) + add exclusion role

User asked why FINAL_decision.xlsx had fewer cell types than the original v6 input
workbook. Reason: anchor CSV was a small subset. Now expanded to cover all 24 cell
types from the v6 image (12 missing types restored + 4 CA types kept).

Cell types added to celltype_to_subclass_anchor.csv (12 new labels, 34 new rows):
  CP:    Patch/striosome SPN, Matrix/exopatch SPN, PV/SST/NPY interneurons (3)
  BMAp:  BMA/MEA VGLUT2-like excitatory (NEW target),
         Amygdala GABA / striatal-like inhibitory neighbors (NEW exclusion) (2)
  RE:    Reticular/inhibitory thalamic exclusion (NEW exclusion) (1)
  LM:    Nearby hypothalamic exclusion populations (NEW exclusion) (1)
  ORBm:  L5 IT / L5 ET/PT output neurons, L6 CT / L6b, Cortical interneurons (3)
  AId:   Deep-layer output neurons, Cortical interneurons (2)

Schema changes:
  - celltype_to_subclass_anchor.csv: NEW column 'role' = 'target' (default) or
    'exclusion_counterstain' (used as negative control)
  - curated_marker_template.csv: 12 new rows with positive + exclusion markers
  - Final_Summary sheet: NEW column 'anchor_role'
  - FINAL_Recommendations sheet: NEW column 'role'

Code changes:
  - D02_create_final_probe_workbook.py:
    * Reads optional 'role' from anchor CSV
    * Threads role through Final_Summary rows and rec_rows
    * build_final_recommendations: exclusion rows get
      what_to_do = 'EXCLUSION COUNTER-STAIN (not a probe target; use markers
      to confirm NOT this cell)' and drugs = '(counter-stain only)'
  - D05_make_recommendations_workbook.py:
    * Adds 'role' / 'anchor_role' columns to width tables
    * Grey row fill for EXCLUSION rows (alongside green ORDER, yellow SPATIAL)

Outputs:
  - FINAL_Recommendations: 18 rows x 11 cols  ->  53 rows x 12 cols
  - Decision_Table_full:   18 rows x 15 cols  ->  53 rows x 16 cols
  - 25 ORDER + 20 ORDER WITH SPATIAL + 8 EXCLUSION verdicts (0 REVIEW)

README: added coverage table showing all 24 cell types broken into targets vs
exclusion counter-stains by region.
